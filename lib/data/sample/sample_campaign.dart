@Deprecated(
  'Use package:depense_game/data/campaign/campaign_data.dart instead.',
)
library;

// Backward-compatible export kept temporarily while older imports are migrated.
export '../campaign/campaign_data.dart';
