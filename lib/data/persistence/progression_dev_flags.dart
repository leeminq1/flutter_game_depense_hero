/// Temporary development switches for campaign progression.
///
/// Keep this enabled while Stage 1-30 content is being authored and playtested.
/// Before a public build, set this to `false` so stage unlocks return to the
/// authored progression requirements in the progress stores.
const bool kUnlockAllCampaignStagesForDevelopment = true;
